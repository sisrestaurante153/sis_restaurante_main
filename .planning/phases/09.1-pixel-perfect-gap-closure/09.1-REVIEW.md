---
phase: 09.1-pixel-perfect-gap-closure
reviewed: 2026-04-19T21:40:00Z
depth: standard
files_reviewed: 4
files_reviewed_list:
  - src/app/(app)/itens/novo/page.tsx
  - src/modules/catalog/ui/item-form.tsx
  - src/modules/catalog/ui/purchases-editor.tsx
  - src/modules/engineering/ui/ficha-form.tsx
commits_reviewed:
  - 406f75f # docs(09.1): gap analysis
  - 2eb2206 # fix(09.1): /itens/novo PageHeader size=compact
  - b074c51 # fix(09.1): drop FormSection description on PurchasesEditor
  - b545f46 # fix(09.1): operationalCategory select InputLabelProps shrink
  - 23b1809 # fix(09.1): ficha readonly Cod/Data fields InputLabelProps shrink
  - f8ea30c # chore(09.1): re-capture Phase 9 screenshots post-fix
  - b5cce40 # docs(09.1): close phase 9.1
findings:
  critical: 0
  warning: 0
  info: 0
  total: 0
status: clean
---

# Phase 9.1: Code Review Report

**Reviewed:** 2026-04-19T21:40:00Z
**Depth:** standard
**Files Reviewed:** 4 (source) + 4 (screenshots, binary) + 2 (planning docs)
**Status:** clean

## Summary

Phase 9.1 applies four atomic UI fixes to close pixel-perfect gaps identified
post-Phase 9 closure. All fixes reviewed at `standard` depth for correctness,
contract adherence (D-01..D-20), scope hygiene, and regression risk.

**Result: CLEAN. Zero findings across all severity levels.**

### Correctness verification

1. **`size="compact"` on PageHeader** (`2eb2206`) — prop supported by
   `PageHeader.tsx:27` (`size?: "default" | "compact"`). Aligns `/itens/novo`
   with `/itens/[id]` (already compact). Satisfies HTML contract (title 22px).

2. **`InputLabelProps={{ shrink: true }}` on operationalCategory select**
   (`b545f46`) — canonical MUI pattern to force the label into the raised state
   regardless of the value/option match. Correctly addresses the case where
   DB-driven `operationalCategory` doesn't exist in `resolvedOperationalCategoryOptions`.
   No accessibility regression: `label` prop remains, so MUI continues to emit
   `htmlFor`/`aria-labelledby` linkage. `shrink` only controls visual position.

3. **`InputLabelProps={{ shrink: true }}` on readonly Cod./Data TextFields**
   (`23b1809`) — same MUI pattern applied to `ReadonlyTextField` helper (line 37)
   and the inline Cod. field (line 256). Necessary because readonly fields with
   empty values on `/nova` would collapse the label into the placeholder area,
   breaking HTML `.f label` static-above contract. Fix is minimal and surgical.

4. **Drop `description` prop from PurchasesEditor FormSection** (`b074c51`) —
   `FormSection.tsx:29` already guards with `description ? ... : null`, so
   dropping the prop is safe (no undefined render). Extends D-13
   ("FormSection description drop") to Detalhamento block consistently.

### Contract adherence (D-01..D-20)

- **D-02 preserved** — MUI TextField retained; fixes applied via
  `InputLabelProps` prop, not migration to native input.
- **D-13 extended** — description drop completed on PurchasesEditor, closing
  the scope gap from original Phase 9 execution (only Identificacao and
  Observacoes had been cleaned).
- **D-14, D-16, D-05, D-06, D-08** — not touched by 9.1; preserved verbatim.
- **No new decisions introduced** — 9.1 is pure gap closure, no D-21+.

### Regression check

- `npx vitest run src/tests/unit/item-form.test.tsx src/tests/unit/ficha-form.test.tsx`:
  **19/19 PASS** (item-form: 4, ficha-form: 15). No regressions from the
  `InputLabelProps` additions or the FormSection description drop.
- No changes to grid-template-columns literals, hex tokens, or prop contracts
  that would invalidate pixel-perfect E2E specs (`tests/e2e/pixel-perfect-phase8.spec.ts`).

### Scope hygiene

- **STATE.md**: untouched (zero diff).
- **ROADMAP.md**: untouched (zero diff).
- **Screenshots**: correctly re-captured for 4 target screens
  (`01-itens-novo.png`, `02-itens-edit.png`, `03-fichas-nova.png`,
  `04-fichas-edit.png`). A stale `02-itens-novo.png` was deleted in the same
  commit (`f8ea30c`), which is correct cleanup — the convention is
  `01-itens-novo` / `02-itens-edit` (numeric prefix indexes screen, not role).
- Each fix is a separate atomic commit with a Conventional Commit message and
  inline comment citing the gap number and HTML line reference. Excellent
  traceability.

### Remaining gaps (documented, not blocking)

Gaps #3, #4, #9, #10 are correctly deferred (by design / designer discretion /
out of HTML contract). Rationale recorded in `09.1-GAPS.md` lines 70-75 and
`09.1-SUMMARY.md` "Gaps remaining" table. No P0 remaining.

## Critical Issues

None.

## Warnings

None.

## Info

None. Commits are tight, comments cite source-of-truth HTML line numbers,
MUI API usage is canonical, tests pass, scope is respected.

---

**Commits reviewed:** `406f75f..b5cce40` (7 commits; range
`6baca18..HEAD`).

_Reviewed: 2026-04-19T21:40:00Z_
_Reviewer: Claude (gsd-code-reviewer)_
_Depth: standard_
