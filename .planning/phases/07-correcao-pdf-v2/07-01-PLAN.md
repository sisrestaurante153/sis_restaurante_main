---
phase: 07-correcao-pdf-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/middleware.ts
  - src/app/api/auth/login/route.ts
  - src/modules/platform/server/env.ts
  - src/modules/engineering/server/engineering-repository.ts
  - src/modules/engineering/ui/TotaisIndicadores.tsx
  - src/tests/unit/session.test.ts
  - src/tests/unit/auth-routes.test.ts
  - src/tests/unit/ficha-form.test.tsx
autonomous: true
requirements: [PDFV2-CRIT-03, PDFV2-CRIT-04, PDFV2-CRIT-05, PDFV2-CRIT-06, PDFV2-CRIT-07]
user_setup: []
must_haves:
  truths:
    - "Nenhum runtime usa segredo hardcoded para assinar sessao"
    - "Venda de referencia exibe -- quando o valor de venda nao e valido"
    - "CMV total exibe Calcular peso quando o peso final nao for utilizavel"
    - "Margem de contribuicao e CMV da marmita nao ficam em branco por valores nao finitos"
  artifacts:
    - path: "src/modules/platform/server/env.ts"
      provides: "Leitura obrigatoria e centralizada de SESSION_SECRET"
      contains: "getRequiredSessionSecret"
    - path: "src/modules/engineering/ui/TotaisIndicadores.tsx"
      provides: "Guardas de renderizacao para metricas comerciais"
      contains: "Number.isFinite"
  key_links:
    - from: "src/middleware.ts"
      to: "src/modules/platform/server/env.ts"
      via: "helper de segredo obrigatorio"
      pattern: "getRequiredSessionSecret"
    - from: "src/app/api/auth/login/route.ts"
      to: "src/modules/platform/server/env.ts"
      via: "helper de segredo obrigatorio"
      pattern: "getRequiredSessionSecret"
---

<objective>
Eliminar os riscos imediatos de seguranca e os erros numericos visiveis do PDF v2.

Purpose: travar os P1 que podem comprometer sessao e confianca do custo antes das refatoracoes estruturais.
Output: runtime sem segredo hardcoded e Quadro Final blindado contra `NaN`, vazios e divisoes invalidas.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-correcao-pdf-v2/07-CONTEXT.md
@.planning/phases/07-correcao-pdf-v2/07-RESEARCH.md
@src/middleware.ts
@src/app/api/auth/login/route.ts
@src/modules/platform/server/env.ts
@src/modules/engineering/server/engineering-repository.ts
@src/modules/engineering/ui/TotaisIndicadores.tsx
@src/tests/unit/session.test.ts
@src/tests/unit/auth-routes.test.ts
@src/tests/unit/ficha-form.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover fallback hardcoded de SESSION_SECRET</name>
  <files>src/middleware.ts, src/app/api/auth/login/route.ts, src/modules/platform/server/env.ts, src/tests/unit/session.test.ts, src/tests/unit/auth-routes.test.ts</files>
  <read_first>src/middleware.ts, src/app/api/auth/login/route.ts, src/modules/platform/server/env.ts, src/modules/access/server/session.ts, src/tests/unit/session.test.ts, src/tests/unit/auth-routes.test.ts, .env.example</read_first>
  <action>Criar `export function getRequiredSessionSecret()` em `src/modules/platform/server/env.ts`. A funcao deve ler `SESSION_SECRET`, retornar o valor apenas quando nao estiver vazio e lancar `Error("SESSION_SECRET is required.")` quando ausente. Substituir as duas ocorrencias de `process.env.SESSION_SECRET ?? "0123456789abcdef0123456789abcdef"` em `src/middleware.ts` e `src/app/api/auth/login/route.ts` por esse helper centralizado. Atualizar os testes de sessao e de rota de auth para injetarem `SESSION_SECRET` explicitamente no ambiente de teste, sem depender de segredo literal no runtime.</action>
  <verify>npx vitest run src/tests/unit/session.test.ts src/tests/unit/auth-routes.test.ts</verify>
  <acceptance_criteria>
    - src/middleware.ts does not contain `0123456789abcdef0123456789abcdef`
    - src/app/api/auth/login/route.ts does not contain `0123456789abcdef0123456789abcdef`
    - src/modules/platform/server/env.ts contains `export function getRequiredSessionSecret`
    - src/tests/unit/auth-routes.test.ts contains `SESSION_SECRET`
  </acceptance_criteria>
  <done>Os pontos de login e middleware passam a depender do helper obrigatorio e os testes continuam verdes com segredo fornecido pelo ambiente de teste.</done>
</task>

<task type="auto">
  <name>Task 2: Blindar metricas comerciais contra valores nao finitos e divisoes invalidas</name>
  <files>src/modules/engineering/server/engineering-repository.ts, src/modules/engineering/ui/TotaisIndicadores.tsx, src/tests/unit/ficha-form.test.tsx</files>
  <read_first>src/modules/engineering/server/engineering-repository.ts, src/modules/engineering/ui/TotaisIndicadores.tsx, src/tests/unit/ficha-form.test.tsx, src/modules/engineering/ui/components-editor.tsx</read_first>
  <action>Normalizar `salePrice`, `variableExpensePercent`, `cmvPerKg`, `mealCmv`, `contributionMarginValue`, `contributionMarginPercent` e derivados para que qualquer numero nao finito resulte em fallback textual coerente no presenter. Em `src/modules/engineering/server/engineering-repository.ts`, ajustar `buildCommercialSummary()` para propagar apenas strings validas, usando `Calcular peso` quando a linha depender de `Peso Final` invalido e `--` ou `Informe o valor` quando a linha depender de `Preco de venda` invalido. Em `src/modules/engineering/ui/TotaisIndicadores.tsx`, alterar `formatCurrency()` e as linhas de `Venda de referencia`, `CMV total`, `Margem de contribuicao` e `CMV da marmita` para usar `Number.isFinite(...)` antes de formatar. Atualizar o teste da ficha para cobrir os fallbacks `--`, `Calcular peso` e ausencia de campos em branco.</action>
  <verify>npx vitest run src/tests/unit/ficha-form.test.tsx</verify>
  <acceptance_criteria>
    - src/modules/engineering/ui/TotaisIndicadores.tsx contains `Number.isFinite`
    - src/modules/engineering/ui/TotaisIndicadores.tsx contains `Venda de referencia`
    - src/modules/engineering/ui/TotaisIndicadores.tsx contains `CMV total`
    - src/modules/engineering/ui/TotaisIndicadores.tsx contains `Margem de contribuicao`
    - src/modules/engineering/server/engineering-repository.ts contains `formatOptionalMetric`
    - src/modules/engineering/server/engineering-repository.ts contains `Calcular peso`
    - src/tests/unit/ficha-form.test.tsx contains `Calcular peso`
  </acceptance_criteria>
  <done>O Quadro Final mostra fallbacks coerentes para venda invalida, peso final ausente e margens derivadas, sem `NaN` nem campos em branco.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/tests/unit/session.test.ts src/tests/unit/auth-routes.test.ts`
- [ ] `npx vitest run src/tests/unit/ficha-form.test.tsx`
- [ ] `npm run typecheck`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No hardcoded runtime secret remains
- `Venda de referencia`, `CMV total`, `Margem de contribuicao` e `CMV da marmita` nunca ficam com `NaN`, vazio indevido ou divisao invalida sem fallback
</success_criteria>

<output>
After completion, create `.planning/phases/07-correcao-pdf-v2/07-01-SUMMARY.md`
</output>
