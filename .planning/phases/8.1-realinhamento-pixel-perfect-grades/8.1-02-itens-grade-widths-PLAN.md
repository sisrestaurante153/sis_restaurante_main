---
phase: 8.1
plan: 02
plan_id: 8.1-02
description: Realinhar as 2 larguras residuais da Grade de Itens (/itens) aos valores hex/px do HTML update/tela-itens-grade-v2.html — colunas Nome (162px fixo, sem flex) e Obs (40px) — apenas CSS.
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/catalog/ui/items-listing-view.tsx
  - src/tests/unit/items-listing.test.tsx
autonomous: true
atomic_commit: "fix(8.1-02): align itens grade Nome/Obs widths to HTML reference"
requirements:
  - SPEC-4-TELAS-ESTRITO
tags:
  - catalog
  - grid
  - pixel-perfect
must_haves:
  truths:
    - "Coluna Nome (field=name) da Grade de Itens usa largura fixa 162px (sem flex), conforme HTML update/tela-itens-grade-v2.html linha 57."
    - "Coluna Obs (field=description) usa largura exata 40px conforme HTML linha 70 (ja em 40, guardar via test)."
    - "Delta Nome +98px (causado por flex:1 consumindo leftover) e delta Obs +10px zerados — ambas dentro de +/- 4px."
    - "Nenhuma mudanca em renderCell, logica ou dados — somente propriedades width/flex."
  artifacts:
    - path: "src/modules/catalog/ui/items-listing-view.tsx"
      provides: "Coluna name com width: 162 sem flex; coluna description com width: 40"
      contains: "width: 162"
    - path: "src/tests/unit/items-listing.test.tsx"
      provides: "Unit test guardando Nome=162 e Obs=40 (+/- 4px) contra HTML"
      min_lines: 20
  key_links:
    - from: "items-listing-view.tsx columns[] field=name"
      to: "update/tela-itens-grade-v2.html linha 57 col.c-nome"
      via: "largura numerica literal"
      pattern: "width: 162"
    - from: "items-listing-view.tsx columns[] field=description"
      to: "update/tela-itens-grade-v2.html linha 70 col.c-obs"
      via: "largura numerica literal"
      pattern: "width: 40"
---

<objective>
Fechar os 2 deltas residuais da Grade de Itens (/itens) descobertos pelo sweep Playwright do
plano 08-07: Nome +98px e Obs +10px. O delta Nome vem do `flex: 1` na columnDef atual que
consome o espaco horizontal restante; remover flex e fixar em 162px conforme HTML
`update/tela-itens-grade-v2.html` linha 57.

Purpose: cumprir criterio 2 da Phase 8.1 ("Grade de itens: colunas Nome e Obs alinhadas ao
HTML — deltas +98 e +10 zerados") mantendo a grade de 15 colunas intacta (Codigo, Nome, Tipo,
Categoria, QtdeCompra, UnCompra, PrecoCompra, Fator, QtdeUso, UnUso, PrecoUso, Fornecedor,
Status, Data, Obs).

Output: `items-listing-view.tsx` com `name` em largura fixa 162px (sem flex) + unit test
existente (`items-listing.test.tsx`) estendido com guard de larguras Nome/Obs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-recuperacao-fidelidade-layout-e-modelo-fornecedor/VERIFICATION.md
@src/modules/catalog/ui/items-listing-view.tsx
@src/tests/unit/items-listing.test.tsx
@update/tela-itens-grade-v2.html
@tests/e2e/pixel-perfect-phase8.spec.ts

<interfaces>
<!-- HTML source of truth: update/tela-itens-grade-v2.html linhas 56-70 -->

```css
col.c-cod  {width:72px}   /* Codigo       linha 56 */
col.c-nome {width:162px}  /* Nome         linha 57 */  <-- ALVO
col.c-tipo {width:92px}   /* Tipo         linha 58 */
col.c-cat  {width:102px}  /* Categoria    linha 59 */
col.c-qtdc {width:72px}   /* Qtde Compra  linha 60 */
col.c-unc  {width:54px}   /* Un. Compra   linha 61 */
col.c-precc{width:74px}   /* Preco Compra linha 62 */
col.c-fator{width:62px}   /* Fator        linha 63 */
col.c-qtdu {width:64px}   /* Qtde Uso     linha 64 */
col.c-unu  {width:50px}   /* Un. Uso      linha 65 */
col.c-precu{width:74px}   /* Preco Uso    linha 66 */
col.c-forn {width:114px}  /* Fornecedor   linha 67 */
col.c-sta  {width:64px}   /* Status       linha 68 */
col.c-data {width:92px}   /* Ult. Atual.  linha 69 */
col.c-obs  {width:40px}   /* Obs          linha 70 */  <-- ALVO
```

Estado atual (items-listing-view.tsx):
- field=name       width: 160, flex: 1  (linhas 286-289)  → **width: 162, remover flex** (target 162, delta atual +98 por flex)
- field=description width: 40           (linha 473)       → **width: 40** (ja OK; apenas guardar por test; delta reportado +10 resulta de flex na coluna Nome empurrando layout — zera quando flex removido)

As outras 13 colunas ja estao dentro de +/- 4px do HTML conforme sweep do 08-07. Nao tocar.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Estender items-listing.test.tsx com guard de larguras Nome=162 e Obs=40</name>
  <files>src/tests/unit/items-listing.test.tsx</files>
  <read_first>
    - src/tests/unit/items-listing.test.tsx (integral — entender setup, imports, padrao de render existente)
    - src/modules/catalog/ui/items-listing-view.tsx (linhas 277-475 — columns useMemo, focar em name linha 286-295 e description linha 470-475)
    - update/tela-itens-grade-v2.html (linhas 56-70 — colgroup)
    - src/tests/unit/fichas-listing.test.tsx (se ja criado no 8.1-01 — reusar padrao de assertion)
  </read_first>
  <behavior>
    - Test A: coluna Nome (field=name) tem largura 162px (+/- 4px).
    - Test B: coluna Obs (field=description) tem largura 40px (+/- 4px).
    - Test C: coluna name NAO tem propriedade `flex` (undefined) — largura e fixa.
  </behavior>
  <action>
Adicionar um novo `describe('Grade de Itens — larguras pixel-perfect residuais
(SPEC-4-TELAS-ESTRITO)', ...)` no fim de `src/tests/unit/items-listing.test.tsx` (nao tocar
nos describes existentes; manter backward compat).

Estrategia de assertion: mesma escolhida no plano 8.1-01 (1A via render DOM, ou 1B via export
de constante). Usar a MESMA estrategia aplicada no 8.1-01 para consistencia — se 8.1-01
precisou refatorar para exportar columns spec, fazer o mesmo aqui.

Skeleton (1A — render + DOM):
```tsx
import { render, screen } from '@testing-library/react';
import { ItemsListingView } from '@/modules/catalog/ui/items-listing-view';

describe('Grade de Itens — larguras pixel-perfect residuais (SPEC-4-TELAS-ESTRITO)', () => {
  const TARGET = { name: 162, description: 40 } as const;

  it.each(Object.entries(TARGET))(
    'coluna %s tem largura %ipx (+/- 4px) alinhada ao HTML update/tela-itens-grade-v2.html',
    async (field, targetWidth) => {
      render(<ItemsListingView rows={[]} /* props minimas existentes */ />);
      const label = field === 'name' ? /Nome do Item/i : /^Obs$/i;
      const header = await screen.findByRole('columnheader', { name: label });
      const computedWidth = Number(header.getAttribute('aria-colwidth') ?? header.style.width.replace('px',''));
      expect(Math.abs(computedWidth - targetWidth)).toBeLessThanOrEqual(4);
    }
  );

  it('coluna name NAO possui flex (largura fixa)', () => {
    // Refletir via spec exposta OR snapshot DOM width stability apos dois renders com viewports diferentes
    // ...
  });
});
```

Nota sobre test C (flex): se estrategia 1A nao permite verificar `flex` diretamente (flex
afeta apenas layout computado), converter em um smoke que renderize com 2 viewports e
assert que `aria-colwidth` permaneca constante (indicativo de width fixo). Se preferir
simplicidade, refatorar items-listing-view para expor `export const ITEMS_COLUMNS` spec e
assertar `ITEMS_COLUMNS.find(c => c.field === 'name')?.flex === undefined`.

Commit RED: `test(8.1-02): add RED guard for itens grade Nome/Obs widths`

Rodar:
```
npm run test:unit -- items-listing
```
Esperar FAIL no test de `name` (atualmente 160+flex → computado ~258). Obs pode passar se
`width: 40` estiver ativo.
  </action>
  <verify>
    <automated>npm run test:unit -- items-listing 2>&1 | tee .planning/phases/8.1-realinhamento-pixel-perfect-grades/artifacts/t1-red-8.1-02.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SPEC-4-TELAS-ESTRITO" src/tests/unit/items-listing.test.tsx` >= 1
    - `grep -c "name: 162" src/tests/unit/items-listing.test.tsx` >= 1
    - `grep -c "description: 40" src/tests/unit/items-listing.test.tsx` >= 1
    - `npm run test:unit -- items-listing` reporta pelo menos 1 FAIL (coluna name)
    - Commit `test(8.1-02): add RED guard for itens grade Nome/Obs widths` criado
  </acceptance_criteria>
  <done>Test RED pronto; proximo task GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Remover flex e fixar width: 162 em field=name; confirmar description=40</name>
  <files>src/modules/catalog/ui/items-listing-view.tsx</files>
  <read_first>
    - src/modules/catalog/ui/items-listing-view.tsx (linhas 277-475 — revisar columns useMemo completo antes de tocar)
    - update/tela-itens-grade-v2.html (linhas 57 e 70 — confirmar targets)
    - src/tests/unit/items-listing.test.tsx (RED da Task 1)
  </read_first>
  <action>
**A) Linha 286-295 (field=name):**

Substituir:
```ts
{
  field: "name",
  headerName: "Nome do Item",
  width: 160,
  flex: 1,
  sortable: true,
  renderCell: ({ value }: GridRenderCellParams<ItemRow, string>) => (
    <Typography fontWeight={700} noWrap>
      {value}
    </Typography>
  )
},
```

Por:
```ts
{
  field: "name",
  headerName: "Nome do Item",
  width: 162,             // HTML linha 57: col.c-nome {width:162px}
  sortable: true,
  renderCell: ({ value }: GridRenderCellParams<ItemRow, string>) => (
    <Typography fontWeight={700} noWrap>
      {value}
    </Typography>
  )
},
```

Mudancas: `width: 160` → `width: 162`; remover linha `flex: 1,`. NADA MAIS.

**B) Linha 471-475 (field=description):**

Confirmar que `width: 40` ja esta presente (linha 473). Se ja esta, nao tocar. Se por algum
motivo o grep falhar, ajustar para `width: 40` (HTML linha 70: `col.c-obs {width:40px}`).

**C) Rodar suite:**
```
npm run test:unit -- items-listing
npm run typecheck
```

Ambos 12/12 (ou N/N) GREEN.

**D) Subset E2E:**
```
npx playwright test tests/e2e/pixel-perfect-phase8.spec.ts -g "itens" --workers=1
```

**E) Regressao estavel:**
```
npx playwright test tests/e2e/bootstrap.spec.ts tests/e2e/navigation.spec.ts --workers=1
```

Commit: `fix(8.1-02): align itens grade Nome/Obs widths to HTML reference`.
  </action>
  <verify>
    <automated>npm run test:unit -- items-listing && npm run typecheck && npx playwright test tests/e2e/pixel-perfect-phase8.spec.ts -g "itens" --workers=1 2>&1 | tee .planning/phases/8.1-realinhamento-pixel-perfect-grades/artifacts/t2-green-8.1-02.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "width: 162" src/modules/catalog/ui/items-listing-view.tsx` >= 1 (name)
    - `grep -c "flex: 1," src/modules/catalog/ui/items-listing-view.tsx` decrementou em 1 vs antes (flex removido APENAS de name — nao tocar em outros flex se existirem; verificar git diff)
    - Em git diff, linha removida `flex: 1,` aparece EXATAMENTE UMA VEZ no bloco field=name (linhas 286-295)
    - `grep -n "width: 40" src/modules/catalog/ui/items-listing-view.tsx` mostra linha 473 (description/Obs)
    - `npm run test:unit -- items-listing` exit 0 (todos GREEN incluindo RED da Task 1)
    - `npm run typecheck` exit 0
    - Subset E2E itens-grade de pixel-perfect-phase8.spec.ts PASS
    - Bootstrap + navigation E2E PASS (zero regressao)
  </acceptance_criteria>
  <done>Grade de itens Nome/Obs alinhadas ao HTML; unit + E2E subset GREEN; commit atomico.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | CSS-only refactor em componente ja autorizado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8.1-02-01 | N/A | CSS-only refactor em items-listing-view; nenhuma mudanca em query/dados | accept | Reuso de authz da rota `/itens`. ASVS L1 V1.1. |
</threat_model>

<verification>
- `npm run test:unit -- items-listing` exit 0
- `npm run typecheck` exit 0
- `npx playwright test tests/e2e/pixel-perfect-phase8.spec.ts -g "itens" --workers=1` PASS
- `git diff src/modules/catalog/ui/items-listing-view.tsx` mostra APENAS: (1) 160→162 em field=name, (2) remocao de `flex: 1,` em field=name. Zero mudanca em renderCell, imports, logica.
- Smoke visual: `/itens` com DevTools — column name tem ~162px; sem flex consumindo espaco extra.
</verification>

<success_criteria>
1. Coluna Nome tem largura 162px (+/- 4px) — delta +98 zerado (criterio 2 da Phase 8.1).
2. Coluna Obs tem largura 40px (+/- 4px) — delta +10 zerado.
3. Unit test `items-listing.test.tsx` guarda Nome=162 e Obs=40 e passa GREEN.
4. Subset E2E pixel-perfect-phase8.spec.ts para itens-grade PASS.
5. Zero regressao em bootstrap + navigation.
6. Commit atomico: `fix(8.1-02): align itens grade Nome/Obs widths to HTML reference`.
</success_criteria>

<rollback>
Mudanca e 2 linhas em `items-listing-view.tsx` + bloco describe em `items-listing.test.tsx`.
Reverter via `git revert <sha-8.1-02>` se necessario. Nenhum impacto em dados.
</rollback>

<output>
Apos completar, criar `.planning/phases/8.1-realinhamento-pixel-perfect-grades/8.1-02-itens-grade-widths-SUMMARY.md`
</output>
</content>
