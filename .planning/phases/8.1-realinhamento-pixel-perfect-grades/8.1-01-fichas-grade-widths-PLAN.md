---
phase: 8.1
plan: 01
plan_id: 8.1-01
description: Realinhar as 12 larguras da Grade de Fichas (/fichas) aos valores hex/px do HTML update/tela-fichas-grade-v1.html (SPEC-4-TELAS-ESTRITO). Apenas CSS/grid widths — zero mudanca de logica.
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/engineering/ui/fichas-listing-view.tsx
  - src/tests/unit/fichas-listing.test.tsx
autonomous: true
atomic_commit: "fix(8.1-01): align fichas grade column widths to HTML reference"
requirements:
  - SPEC-4-TELAS-ESTRITO
tags:
  - engineering
  - grid
  - pixel-perfect
must_haves:
  truths:
    - "Grade de fichas (/fichas) exibe as 12 colunas com larguras identicas (+/- 4px) as declaradas em update/tela-fichas-grade-v1.html linhas 57-68."
    - "Coluna Produto (itemName) usa largura fixa 170px (sem flex); ver-badge V{n} continua inline."
    - "Nenhuma nova coluna, nenhuma coluna removida, nenhuma mudanca em renderCell alem do necessario para remover flex."
    - "Subset Playwright tests/e2e/pixel-perfect-phase8.spec.ts relativos a fichas-grade passam (deltas Produto/Modalidade/Grupo/Componentes/FC/IC/Custo/Data/Status/Obs zerados)."
  artifacts:
    - path: "src/modules/engineering/ui/fichas-listing-view.tsx"
      provides: "12 GridColDef com width numerico fixo conforme HTML; itemName SEM flex"
      contains: "width: 170"
    - path: "src/tests/unit/fichas-listing.test.tsx"
      provides: "Guarda unit test que assert 12 larguras contra constantes derivadas do HTML"
      min_lines: 30
  key_links:
    - from: "fichas-listing-view.tsx columns[]"
      to: "update/tela-fichas-grade-v1.html colgroup (linhas 57-68)"
      via: "largura numerica literal por field"
      pattern: "width: (60|170|100|82|60|60|82|80|110|64|38)"
    - from: "src/tests/unit/fichas-listing.test.tsx"
      to: "fichas-listing-view columns spec"
      via: "import e assertion por field.width"
      pattern: "expect.*width.*toBe"
---

<objective>
Fechar o gap pixel-perfect da Grade de Fichas (/fichas) descoberto pelo sweep Playwright do
plano 08-07 (13 FAIL contra 26 PASS). Zerar deltas de +12 a +70 px em 10 colunas sem adicionar
feature e sem alterar logica de negocio. SPEC-4-TELAS-ESTRITO exige que a grade siga
estritamente o HTML `update/tela-fichas-grade-v1.html` (contrato aprovado).

Purpose: cumprir os criterios de sucesso 1 e 3 da Phase 8.1 (12/12 colunas +/- 4px contra HTML;
tests/e2e/pixel-perfect-phase8.spec.ts passa para fichas-grade).

Output: `fichas-listing-view.tsx` com larguras fixas alinhadas ao HTML + unit test guardando
larguras por field + E2E subset green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-recuperacao-fidelidade-layout-e-modelo-fornecedor/VERIFICATION.md
@src/modules/engineering/ui/fichas-listing-view.tsx
@update/tela-fichas-grade-v1.html
@tests/e2e/pixel-perfect-phase8.spec.ts

<interfaces>
<!-- HTML source of truth: update/tela-fichas-grade-v1.html linhas 57-68 -->

```css
col.c-cod  {width:60px}   /* Codigo       */
col.c-prod {width:170px}  /* Produto      */
col.c-mod  {width:100px}  /* Modalidade   */
col.c-grp  {width:100px}  /* Grupo Op.    */
col.c-comp {width:82px}   /* Componentes  */
col.c-fc   {width:60px}   /* FC           */
col.c-ic   {width:60px}   /* IC           */
col.c-custo{width:82px}   /* Custo Total  */
col.c-pv   {width:80px}   /* Preco Venda  */
col.c-data {width:110px}  /* Ult. Atual.  */
col.c-sta  {width:64px}   /* Status       */
col.c-obs  {width:38px}   /* Obs          */
```

Estado atual (fichas-listing-view.tsx — field → width atual → target):
- code            60   → 60    ✓ (sem mudanca)
- itemName        flex 1.3 + minWidth 240  → **width: 170 fixo (remover flex e minWidth)**
- modalityLabel   130  → 100
- groupOperational 170 → 100
- status          120  → 64
- componentCount  130  → 82
- correctionFactor 90  → 60
- cookingIndex    90   → 60
- totalCost       130  → 82
- sellingPrice    80   → 80    ✓ (sem mudanca)
- updatedAt       160  → 110
- notes           38   → 38    ✓ (sem mudanca)

Nota importante: a ORDEM atual das colunas em `fichas-listing-view.tsx` (code, itemName,
modalityLabel, groupOperational, **status**, componentCount, FC, IC, totalCost, sellingPrice,
updatedAt, notes) diverge da ordem do HTML (status e penultimo no HTML). O roadmap pede
apenas realinhamento de LARGURAS +/- 4px — NAO tocar na ordem de colunas (nao e escopo).
Confirmar com spec E2E `pixel-perfect-phase8.spec.ts` se os asserts sao por field+width ou
por posicao visual; se por posicao, levantar gap para revisao — nao reordenar por conta propria.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Unit test guardando larguras das 12 colunas contra constantes HTML</name>
  <files>src/tests/unit/fichas-listing.test.tsx</files>
  <read_first>
    - src/tests/unit/fichas-listing.test.tsx (integral — entender setup existente, se existir; se nao existir, criar do zero seguindo padrao de src/tests/unit/items-listing.test.tsx)
    - src/modules/engineering/ui/fichas-listing-view.tsx (linhas 197-391 — columns useMemo)
    - update/tela-fichas-grade-v1.html (linhas 57-68 — colgroup widths)
    - src/tests/unit/app-data-grid-pattern.test.tsx (se existir — referencia de como se testa DataGrid columns)
  </read_first>
  <behavior>
    - Test 1: exporta ou expoe columns spec para poder assertar por field → width.
    - Test 2: para cada um dos 12 fields (code, itemName, modalityLabel, groupOperational, status, componentCount, correctionFactor, cookingIndex, totalCost, sellingPrice, updatedAt, notes), a largura esta dentro de +/- 4px do valor HTML.
    - Test 3: itemName NAO tem `flex` (undefined) — largura e fixa.
  </behavior>
  <action>
Criar (ou estender) `src/tests/unit/fichas-listing.test.tsx` com bloco `describe('Grade de
Fichas — larguras pixel-perfect (SPEC-4-TELAS-ESTRITO)', ...)`.

Como `fichas-listing-view.tsx` define `columns` dentro de `useMemo` no componente, ha duas
opcoes — escolher 1A (preferencial) por minimalismo:

1A. **Assertar via render + query de DOM**: renderizar o componente com `render(<FichasListingView
    rows={...} />)` e consultar headers por `role="columnheader"` + `style.width` (MUI DataGrid
    expoe width computado no DOM como `min-width`/`max-width` style inline no header cell).

1B. **Expor columns via export**: refatorar `fichas-listing-view.tsx` para exportar uma
    constante `FICHAS_COLUMN_WIDTHS: Record<string, number>` e assertar contra ela no test.
    Requer tocar em fichas-listing-view.tsx na Task 1 (invade GREEN). Evitar.

Usar 1A. Skeleton do test:

```tsx
import { render, screen } from '@testing-library/react';
import { FichasListingView } from '@/modules/engineering/ui/fichas-listing-view';

const HTML_WIDTHS: Record<string, number> = {
  code: 60,
  itemName: 170,
  modalityLabel: 100,
  groupOperational: 100,
  status: 64,
  componentCount: 82,
  correctionFactor: 60,
  cookingIndex: 60,
  totalCost: 82,
  sellingPrice: 80,
  updatedAt: 110,
  notes: 38,
};

describe('Grade de Fichas — larguras pixel-perfect (SPEC-4-TELAS-ESTRITO)', () => {
  it.each(Object.entries(HTML_WIDTHS))(
    'coluna %s tem largura %ipx (+/- 4px) alinhada ao HTML update/tela-fichas-grade-v1.html',
    async (field, targetWidth) => {
      render(<FichasListingView rows={[]} /* props minimas */ />);
      const header = await screen.findByRole('columnheader', { name: new RegExp(headerLabel(field), 'i') });
      const computedWidth = Number(header.getAttribute('aria-colwidth') ?? header.style.width.replace('px',''));
      expect(Math.abs(computedWidth - targetWidth)).toBeLessThanOrEqual(4);
    }
  );
});
```

Se MUI DataGrid nao expoe aria-colwidth de forma fiavel no JSDOM, usar alternative 1B —
refatoracao minima: mover array `columns` para fora do useMemo como `export const FICHAS_COLUMNS`
e o useMemo apenas retorna `FICHAS_COLUMNS`. Entao assertar `FICHAS_COLUMNS.find(c => c.field === 'code').width === 60` etc.

Commit RED: `test(8.1-01): add RED guard for fichas grade column widths`

Rodar:
```
npm run test:unit -- fichas-listing
```
Esperar FAIL em ~9 das 12 assertions (deltas atuais). NAO implementar ainda.
  </action>
  <verify>
    <automated>npm run test:unit -- fichas-listing 2>&1 | tee .planning/phases/8.1-realinhamento-pixel-perfect-grades/artifacts/t1-red-8.1-01.log</automated>
  </verify>
  <acceptance_criteria>
    - `src/tests/unit/fichas-listing.test.tsx` existe com describe "SPEC-4-TELAS-ESTRITO"
    - `grep -c 'HTML_WIDTHS' src/tests/unit/fichas-listing.test.tsx` >= 1
    - `grep -c "itemName.*170" src/tests/unit/fichas-listing.test.tsx` >= 1
    - test RUN mostra pelo menos 3 assertions FAILING (deltas atuais de Modalidade/Grupo/Status/Componentes/FC/IC/Custo/Data)
    - Commit `test(8.1-01): add RED guard for fichas grade column widths` criado
  </acceptance_criteria>
  <done>Test RED pronto; proximo task implementa GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Ajustar larguras das 12 colunas em fichas-listing-view.tsx para HTML target</name>
  <files>src/modules/engineering/ui/fichas-listing-view.tsx</files>
  <read_first>
    - src/modules/engineering/ui/fichas-listing-view.tsx (integral — entender renderCell de cada coluna antes de mexer na width)
    - update/tela-fichas-grade-v1.html (linhas 57-68 — colgroup authoritative)
    - src/tests/unit/fichas-listing.test.tsx (RED da Task 1)
  </read_first>
  <action>
Aplicar as mudancas de largura — **APENAS a propriedade `width`** (e remover `flex` + `minWidth`
do `itemName`). NAO tocar em renderCell, headerName, align, sortable, field.

Mapa exato (field → mudanca):

| Field              | Antes                          | Depois (HTML)          | Linha aprox. |
|--------------------|--------------------------------|------------------------|--------------|
| code               | width: 60                      | width: 60 (sem mudanca)| 202          |
| itemName           | minWidth: 240, flex: 1.3       | width: 170             | 213-214      |
| modalityLabel      | width: 130                     | width: 100             | 257          |
| groupOperational   | width: 170                     | width: 100             | 263          |
| status             | width: 120                     | width: 64              | 269          |
| componentCount     | width: 130                     | width: 82              | 280          |
| correctionFactor   | width: 90                      | width: 60              | 293          |
| cookingIndex       | width: 90                      | width: 60              | 308          |
| totalCost          | width: 130                     | width: 82              | 323          |
| sellingPrice       | width: 80                      | width: 80 (sem mudanca)| 338          |
| updatedAt          | width: 160                     | width: 110             | 362          |
| notes              | width: 38                      | width: 38 (sem mudanca)| 373          |

Justificativa HTML por linha:
- code 60 → HTML linha 57 `col.c-cod {width:60px}`
- itemName 170 → HTML linha 58 `col.c-prod {width:170px}`
- modalityLabel 100 → HTML linha 59 `col.c-mod {width:100px}`
- groupOperational 100 → HTML linha 60 `col.c-grp {width:100px}`
- componentCount 82 → HTML linha 61 `col.c-comp {width:82px}`
- correctionFactor 60 → HTML linha 62 `col.c-fc {width:60px}`
- cookingIndex 60 → HTML linha 63 `col.c-ic {width:60px}`
- totalCost 82 → HTML linha 64 `col.c-custo {width:82px}`
- sellingPrice 80 → HTML linha 65 `col.c-pv {width:80px}`
- updatedAt 110 → HTML linha 66 `col.c-data {width:110px}`
- status 64 → HTML linha 67 `col.c-sta {width:64px}`
- notes 38 → HTML linha 68 `col.c-obs {width:38px}`

**itemName (Produto):** substituir
```ts
minWidth: 240,
flex: 1.3,
```
por
```ts
width: 170,
```
(remover tambem `sx={{ width: "100%", ... }}` da Stack interna APENAS se quebrar o ellipsis —
se o ellipsis continua funcional com width fixo, nao tocar; verificar visualmente no smoke).

Rodar:
```
npm run test:unit -- fichas-listing
npm run typecheck
```

Todos os 12 assertions devem passar GREEN. Commit: `fix(8.1-01): align fichas grade column widths to HTML reference`.

Smoke E2E subset:
```
npx playwright test tests/e2e/pixel-perfect-phase8.spec.ts -g "fichas" --workers=1
```

Regressao:
```
npx playwright test tests/e2e/bootstrap.spec.ts tests/e2e/navigation.spec.ts --workers=1
```
  </action>
  <verify>
    <automated>npm run test:unit -- fichas-listing && npm run typecheck && npx playwright test tests/e2e/pixel-perfect-phase8.spec.ts -g "fichas" --workers=1 2>&1 | tee .planning/phases/8.1-realinhamento-pixel-perfect-grades/artifacts/t2-green-8.1-01.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "width: 170" src/modules/engineering/ui/fichas-listing-view.tsx` >= 1 (itemName)
    - `grep -c "flex: 1.3" src/modules/engineering/ui/fichas-listing-view.tsx` returns 0 (flex removido)
    - `grep -c "minWidth: 240" src/modules/engineering/ui/fichas-listing-view.tsx` returns 0
    - `grep -c "width: 100" src/modules/engineering/ui/fichas-listing-view.tsx` >= 2 (modalityLabel + groupOperational)
    - `grep -c "width: 82" src/modules/engineering/ui/fichas-listing-view.tsx` >= 2 (componentCount + totalCost)
    - `grep -c "width: 60" src/modules/engineering/ui/fichas-listing-view.tsx` >= 3 (code + FC + IC)
    - `grep -c "width: 64" src/modules/engineering/ui/fichas-listing-view.tsx` >= 1 (status)
    - `grep -c "width: 110" src/modules/engineering/ui/fichas-listing-view.tsx` >= 1 (updatedAt)
    - `npm run test:unit -- fichas-listing` exit 0 (12/12 GREEN)
    - `npm run typecheck` exit 0
    - Subset E2E fichas-grade do `pixel-perfect-phase8.spec.ts` PASS
  </acceptance_criteria>
  <done>Grade de fichas alinhada ao HTML; unit + E2E subset GREEN; pronto para verificacao final no 8.1-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Plano puramente de CSS/grid widths. Nenhuma mudanca em query, autorizacao ou dados. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8.1-01-01 | N/A | CSS-only refactor em componente UI ja autorizado. Nenhuma nova superficie. | accept | Reuso de authz herdado da rota `/fichas` (sessao + RBAC existentes). ASVS L1 V1.1 — sem nova superficie. |
</threat_model>

<verification>
- `npm run test:unit -- fichas-listing` exit 0
- `npm run typecheck` exit 0
- `npx playwright test tests/e2e/pixel-perfect-phase8.spec.ts -g "fichas" --workers=1` PASS
- Inspecao visual: abrir `/fichas` e comparar larguras com DevTools contra `update/tela-fichas-grade-v1.html`
- `git diff src/modules/engineering/ui/fichas-listing-view.tsx` afeta APENAS propriedades `width`/`flex`/`minWidth` (zero mudanca em renderCell, logica, imports)
</verification>

<success_criteria>
1. Todas as 12 colunas da Grade de Fichas com largura dentro de +/- 4px do HTML (criterio 1 da Phase 8.1).
2. itemName sem flex — largura fixa 170px conforme HTML linha 58.
3. Unit test `fichas-listing.test.tsx` guarda as 12 larguras e passa GREEN.
4. Subset E2E pixel-perfect-phase8.spec.ts para fichas-grade passa.
5. Zero regressao em bootstrap + navigation E2E.
6. Commit atomico: `fix(8.1-01): align fichas grade column widths to HTML reference`.
</success_criteria>

<rollback>
Se GREEN falhar ou regressao aparecer em bootstrap/navigation:
```
git revert <commit-sha-8.1-01>
```
Mudanca e 100% localizada em `fichas-listing-view.tsx` + `fichas-listing.test.tsx`. Nao ha
migration de dados nem mudanca de schema.
</rollback>

<output>
Apos completar, criar `.planning/phases/8.1-realinhamento-pixel-perfect-grades/8.1-01-fichas-grade-widths-SUMMARY.md`
</output>
</content>
