---
phase: 09.2-cliente-ajustes-finais
parent: 09.1-pixel-perfect-gap-closure / 09-detalhe-item-ficha-pixel-perfect
type: gap-closure
origin: user review 2026-04-20 (WhatsApp-style annotated screenshots)
---

# Phase 9.2 — Ajustes Finais do Cliente

Origem: Cliente revisou os prints de Phase 9.1 e enviou 6 pedidos concretos anotados nos screenshots.
Estes pedidos NÃO alteram escopo de HTML pixel-perfect (alguns até divergem do HTML de referência por preferência do cliente).

## Pedidos

### Tela de Item (/itens/[id] + /itens/novo)

**A1 — Mover "+ Adicionar fornecedor" para o rodapé do card**
- Hoje: botão no cabeçalho do card "Detalhamento de Compras / Fornecedor" (topo direito)
- Pedido: mover para abaixo dos cards de fornecedor (rodapé do card)
- Arquivo: `src/modules/catalog/ui/purchases-editor.tsx` (~linha 202)
- Observação: O HTML de referência (`update/tela-item-v1.html`) TAMBÉM mostra "+ Adicionar fornecedor" no rodapé (linha ~1150). Então este ajuste ALINHA com o HTML.

**A2 — `Unidade de compra` do fornecedor editável**
- Hoje: possivelmente está readonly/disabled no fornecedor principal
- Pedido: deve ser editável
- Arquivo: `src/modules/catalog/ui/purchases-editor.tsx`
- Verificar todos os campos: `purchaseUnit` deve ser editável; se hoje está `disabled`, remover.

### Tela de Ficha (/fichas/[id] + /fichas/nova)

**B1 — 🟡 REMOVER Alert "O item X já aparece na ficha Y"**
- Hoje: dois Alerts inline renderizados acima da tabela de ingredientes quando o item já é usado em outra ficha
- Pedido: remover totalmente
- Arquivos:
  - `src/modules/engineering/ui/components-editor.tsx` (~linha 561) — renderização inline
  - `src/modules/engineering/ui/SimilarFichasBanner.tsx` — componente Banner no FichaFlatGrid
- Decisão: REMOVER tanto o Alert inline em components-editor (warning per-row) quanto o SimilarFichasBanner no FichaFlatGrid. Cliente quer tela limpa sem sugestão de fichas similares.
- Preservar o hook `findFichasUsingItem` mesmo sem uso? NÃO — remover também para evitar dead code.

**B2 — ⚫ Mover "+ Adicionar itens" de Montagem e Descartaveis para o rodapé**
- Hoje: botão no cabeçalho do bloco "Montagem e Descartaveis" (topo direito)
- Pedido: mover para abaixo da tabela de itens de montagem (ou logo depois de "Nenhum ingrediente adicionado a esta etapa ainda." quando vazio)
- Arquivo: `src/modules/engineering/ui/components-editor.tsx` (~linha 609)
- Nota: consistência com A1 — padrão "Adicionar X" sempre no rodapé.

**B3 — ⚫ Trocar ordem: FINALIZACAO deve vir ANTES de QUADRO FINAL**
- Hoje (ordem visual):
  1. Identificacao
  2. ComponentsEditor (Ficha tecnica + Biblioteca + Montagem + **Quadro Final interno**)
  3. Finalizacao
- Pedido:
  1. Identificacao
  2. ComponentsEditor (Ficha tecnica + Biblioteca + Montagem) — **sem Quadro Final**
  3. **Finalizacao** ← sobe
  4. **Quadro Final** ← fica em último (extraido do ComponentsEditor)
- Arquivos:
  - `src/modules/engineering/ui/components-editor.tsx` (~linha 656) — extrair TotaisIndicadores
  - `src/modules/engineering/ui/ficha-form.tsx` — renderizar ordem nova
- Rationale UX: usuário preenche modo de preparo + observações ANTES de ver o cálculo final.
- Propagação: todos os dados que TotaisIndicadores recebe hoje via ComponentsEditor precisam ser passados do ficha-form (state lift).

**B4 — 🟠 Quadro Final 1:1 com o HTML (`update/tela-ficha-tecnica-v2.html`)**
- Hoje: Quadro Final funcional mas com layout não-perfectly alinhado ao HTML
- Pedido: "Tem como ficar o html? (pois o está bem completo)"
- Interpretação: usar o HTML v2 como contrato exato para o Quadro Final — layout, labels, ordem, cálculos, placeholders, cores
- HTML section: `<section class="quadro-final">` em `update/tela-ficha-tecnica-v2.html` (linhas ~440-650)
- Blocos do HTML (ordem e nomes EXATOS):
  1. `PESOS E RENDIMENTO` (Total de peso, Peso util consolidado, Peso final, Fator de Correcao FC, Indice de Coccao IC)
  2. `CUSTOS E CMV` — com botão "Montagem e Descartaveis" no topo direito (deve aparecer igual)
     - Custo total da ficha, CMV sem embalagem, Custo de embalagens e descartaveis, CMV com embalagem, **CMV final aplicado (c/ emb.)** destacado
  3. `VENDA E MARGEM` (Preco de venda com input, Despesa variavel de venda com input, CMV do kg, Despesa variavel aplicada, Margem de contribuicao R$, Margem de contribuicao %, Saude do CMV badge)
  4. `LEITURA OPERACIONAL` (Preco de Referencia, Custo Real da Ficha, Despesa variavel aplicada, Margem de Contribuicao, Diagnostico automatico)
- Arquivo: `src/modules/engineering/ui/TotaisIndicadores.tsx`
- Preservar todo o cálculo existente, apenas ajustar layout/labels/tokens pra 1:1 com HTML.

## Restrições

- Commits atômicos por pedido (A1, A2, B1, B2, B3, B4) — 6 commits mínimo
- Typecheck + unit tests verdes após cada commit
- NÃO tocar STATE.md nem ROADMAP.md
- Rebuild Docker após fixes (`docker compose up -d --build app`) + re-captura dos 4 prints
- Reviewer pass após: code-reviewer + ui-auditor
