---
quick_id: 20260424-ficha-5-ajustes-cliente
mode: quick
date: 2026-04-24
status: complete
commit: 0dd6c34
---

# Quick Task 20260424 — Ficha Técnica: 5 ajustes do cliente

## Resultado

5 ajustes na tela `/fichas/[fichaId]` implementados em um commit atômico. Typecheck clean, 23/23 testes unit passam. Todas as decisões de gray area (picker por código, fórmulas FC/IC, alinhamento Coccao Final) resolvidas na fase de discussão antes do código.

## Decisões travadas (CONTEXT.md)

1. **Picker por código:** Código + Nome lado a lado; digitar código autofill o Item. 72px de largura.
2. **Fórmulas FC/IC:** `output / qtde_usada` (paridade com `buildFichaIndicators` server).
3. **Alinhamento Coccao Final:** reusar o mesmo `GRID_TEMPLATE` das linhas normais (remover `CF_GRID_TEMPLATE`).

## Implementação

### Backend/mappers
- `src/modules/catalog/server/catalog-prisma-mappers.ts`: `mapItemOption` agora expõe `code: item.codigoInterno ?? ""`.
- `src/modules/catalog/server/catalog-repository.ts`: demo `toItemOption` também expõe `code`.
- `src/modules/engineering/ui/components-editor.types.ts`: `code?: string` adicionado a `ComponentOption`.

### UI
- `src/modules/engineering/ui/FichaFlatGrid.tsx` (+199/-45): GRID_TEMPLATE novo 9-col `"22px 72px 1fr 110px 80px 240px 90px 96px 28px"`; header extra "Cod."; subcomponente `CodeInput` com local state + commit-on-blur/Enter; `<TextField select>` para Unidade populado via `unitOptions` com fallback `DEFAULT_UNITS`; FC/IC per-row computado inline; `CF_GRID_TEMPLATE` removido; bloco Coccao Final alinhado ao 9-col grid com IC calculado (`rendimento / soma_qtde`).
- `src/modules/engineering/ui/components-editor.tsx`: prop `unitOptions?: string[]` threaded.
- `src/modules/engineering/ui/ficha-form.tsx`: prop `unitOptions?: string[]` threaded.
- `src/app/(app)/fichas/[fichaId]/page.tsx` + `src/app/(app)/fichas/nova/page.tsx`: `listUnits()` chamado no Promise.all, `unitOptions = units.filter(active).map(code)`.

### Tests
- `src/tests/unit/engineering/FichaFlatGrid.test.tsx`: atualizado para novo GRID_TEMPLATE + sem CF_GRID_TEMPLATE.
- `tests/e2e/pixel-perfect-phase8.spec.ts`: mesmo contrato atualizado.

## Verificação

- `npx tsc --noEmit`: clean, 0 erros ✓
- `npx vitest run src/tests/unit/engineering/FichaFlatGrid.test.tsx src/tests/unit/components-editor.test.tsx src/tests/unit/ficha-form.test.tsx`: **23/23 PASS** em 3.59s
- Diff revisado: 10 arquivos, escopo coeso, zero mudança fora do contrato

## Judgment calls do agent

- Pre-pende MenuItem transiente no select Unidade quando `row.usageUnit` legado (ex: "mg", "maco") não está em `unitOptions` — evita silently-reset do valor ao reabrir ficha antiga.
- `pesoHint` derivado direto do factor (mais limpo que string-prefix extraction).
- `paddingRight: 8` no input Qtde como safety net.
- `minWidth` do container 1040 → 1112 para acomodar a nova coluna Cod.
- Demo `toItemOption` foi atualizado em paralelo — não estava listado no plano explicitamente mas era necessário para demo rodar sem Prisma.

## Ressalvas

- **Schema:** `codigoInterno` já existia em Item — nenhuma migration.
- **Legacy data:** fichas salvas antes deste commit com `usageUnit` fora do master mostrarão opção única no select (evita perda de info).
- **listUnits() fallback:** se falhar/vazio, usa `["kg","g","l","ml","un"]` (paridade com purchases-editor).
- **Mobile:** grid tem `overflowX: "auto"`, nova coluna funciona via scroll horizontal.

## Commits

- `0dd6c34` feat(fichas): 5 ajustes cliente na tela Ficha Tecnica (quick 20260424)
