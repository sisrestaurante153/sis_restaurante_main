---
quick_id: 20260424-ficha-ajustes-fase2
mode: quick
date: 2026-04-24
status: in-progress
must_haves:
  truths:
    - "Coluna Cod. existe na grade Montagem e Descartaveis (IngredienteDataGrid) com input que aceita codigo e autofill o Item"
    - "Aviso fixo 'item ja aparece nesta ficha' removido; substituido por icone de alerta com Tooltip ao lado da linha duplicada (na FichaFlatGrid)"
    - "factorPrefix em FichaFlatGrid retorna 'IF' para limpeza_pre_preparo (antes 'FC') e 'IC' para coccao_preparo"
    - "Custo unit. e Custo insumo (header + valor) usam textAlign:'center' na FichaFlatGrid"
    - "tsc --noEmit passa sem erros"
    - "vitest passa nos testes de FichaFlatGrid"
  artifacts:
    - src/modules/engineering/ui/FichaFlatGrid.tsx
    - src/modules/engineering/ui/IngredienteDataGrid.tsx
    - src/modules/engineering/ui/components-editor.tsx
  key_links:
    - PriorQuick: .planning/quick/20260424-ficha-5-ajustes-cliente/20260424-SUMMARY.md
    - FichaFlatGrid: src/modules/engineering/ui/FichaFlatGrid.tsx
    - IngredienteDataGrid: src/modules/engineering/ui/IngredienteDataGrid.tsx
---

# Quick Task 20260424 — Ficha Tecnica: ajustes fase 2

Continuacao do quick `20260424-ficha-5-ajustes-cliente` (commit 0dd6c34). Cliente revisou e pediu mais 4 ajustes pequenos.

## Ajustes

### Ajuste 1 — COD na grade Montagem e Descartaveis
- File: `src/modules/engineering/ui/IngredienteDataGrid.tsx`
- Adicionar coluna `Cod.` (72px) entre o drag handle e o Item.
- Reutilizar o padrao de `CodeInput` (digitar codigo + commit no blur/Enter -> autofill do Item).
- Atualizar `gridTemplateColumns` (header + linha) e `minWidth` para acomodar.

### Ajuste 2 — Aviso de duplicidade vira Tooltip
- Files: `src/modules/engineering/ui/components-editor.tsx`, `src/modules/engineering/ui/FichaFlatGrid.tsx`
- Remover os `<Alert severity="warning">` que listam itens duplicados (poluiu a tela).
- Trocar o array `duplicateItems` (nomes) por `duplicateItemIds: Set<string>`.
- Em `FichaFlatGrid`, adicionar prop `duplicateItemIds?: Set<string>`. Quando `row.itemId` esta no set, mostrar um icone `WarningAmberIcon` discreto (amber, 14px) ao lado do level chip, dentro de um `<Tooltip title="Este item ja aparece nesta ficha. Revise antes de duplicar." />`.

### Ajuste 3 — Label IC (coccao) vs IF (limpeza)
- File: `src/modules/engineering/ui/FichaFlatGrid.tsx`
- Mudar `factorPrefix`:
  - `limpeza_pre_preparo` -> `"IF"` (antes `"FC"`)
  - `coccao_preparo`      -> `"IC"` (mantem)
- O hint mostrado fica `IF 85%` / `IC 120%` etc.

### Ajuste 4 — Centralizar Custo unit. e Custo insumo
- File: `src/modules/engineering/ui/FichaFlatGrid.tsx`
- Trocar `textAlign: "right"` por `textAlign: "center"` no header das colunas Custo unit. e Custo insumo (linhas ~197-198).
- Trocar `textAlign: "right"` por `textAlign: "center"` nos dois `Box` que renderizam os valores (linhas ~479-485).
- Header e valor ficam centralizados na coluna.

## Tasks (4 commits atomicos)

1. `feat(fichas): add COD column on Montagem e Descartaveis grid (quick fase2 #1)`
2. `feat(fichas): convert duplicate-item warning to row tooltip (quick fase2 #2)`
3. `feat(fichas): rename FC->IF for limpeza factor label (quick fase2 #3)`
4. `style(fichas): center-align Custo unit/Custo insumo columns (quick fase2 #4)`

## Verify
- `npx tsc --noEmit` clean
- `npx vitest run src/tests/unit/engineering/FichaFlatGrid.test.tsx src/tests/unit/components-editor.test.tsx`
